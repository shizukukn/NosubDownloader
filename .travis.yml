language: node_js
node_js:
  - 0.10
before_script:
  - npm install -g grunt-cli
  - npm install -g bower
  - npm install -g tsd@0.6.0-beta.3
  - bower --version
  - tsd --version
  - bower install
  - tsd reinstall
script: npm test